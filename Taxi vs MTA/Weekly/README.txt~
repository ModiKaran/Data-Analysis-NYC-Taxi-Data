Taxi Usage:

We used Map Reduce to compute the weekly taxi usage
These weeks (dates) are in accordance to the weekly turnstile files provided by MTA

MTA Usage:

We used a bash script to calculate total number of rows in weekly MTA turnstile files
For example:
cat 4Jan.txt | wc -l
